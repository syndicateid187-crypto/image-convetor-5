# IMAE Converter - Smart File Converter

This is a full-stack web application for converting various file types (Images, PDF, etc.).

## 🚀 Deployment (Vercel)

This project is configured to work out-of-the-box on Vercel.

### Files included:
- `client/` - Frontend (React + Vite)
- `server/` - Backend (Express logic)
- `server.js` - Root entry point for Vercel/Node
- `vercel.json` - Vercel configuration
- `package.json` - Dependencies and scripts

## 🛠️ Local Setup

1. Install dependencies:
   ```bash
   npm run install-all
   ```

2. Run the application:
   ```bash
   node server.js
   ```

3. Access the application at `http://localhost:5000`.

## 📦 How to Upload to GitHub

If you are uploading manually, make sure to:
1. Upload all folders and files **EXCEPT** `node_modules`.
2. Do not upload `client/dist`.

---
*Created for Satyam*