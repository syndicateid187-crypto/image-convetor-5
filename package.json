{
    "name": "smart-file-converter-ultimate",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "install-all": "npm install && cd client && npm install",
        "build": "cd client && npm run build",
        "start": "node server.js",
        "dev": "concurrently \"npm start\" \"cd client && npm run dev\"",
        "render-postinstall": "cd client && npm install && npm run build"
    },
    "dependencies": {
        "concurrently": "^8.2.2",
        "cors": "^2.8.5",
        "express": "^4.18.2",
        "multer": "^1.4.5-lts.1",
        "pdf-lib": "^1.17.1",
        "pdf-poppler": "^0.2.1",
        "sharp": "^0.33.0"
    }
}